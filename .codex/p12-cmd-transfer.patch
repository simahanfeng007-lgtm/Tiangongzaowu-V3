diff --git a/.github/workflows/p12-retirement-preflight.yml b/.github/workflows/p12-retirement-preflight.yml
index 04b0de0..532d1a5 100644
--- a/.github/workflows/p12-retirement-preflight.yml
+++ b/.github/workflows/p12-retirement-preflight.yml
@@ -1,4 +1,4 @@
-name: P12 R1A repair regression (read-only)
+name: P12 R1A native path closeout (read-only)
 
 on:
   push:
@@ -45,11 +45,11 @@ jobs:
           tests = ['tests/test_composition_execution_manifest_p7d1.py', 'tests/test_tool_manifest_loading_p8.py', 'tests/test_composition_grant_authority_p7d2.py', 'tests/test_composition_step_execution_p7d1.py', 'tests/test_foundation_closeout.py', 'tests/test_p12_retirement_map.py', 'tests/test_p12_retirement_preflight.py']
           for path in tests:
               assert pathlib.Path(path).read_bytes() == subprocess.check_output(['git', 'show', base + ':' + path]), path
-          removed = ['.github/workflows/p12-alias-repair-preparation.yml', '.github/workflows/p12-source-export.yml', '.github/workflows/p12-native-shell-diagnostic.yml', 'scripts/_prepare_p12_alias_repair.py', 'docs/capability-composition/p12-alias-repair.patch', 'docs/capability-composition/p12-alias-tests.py', 'docs/capability-composition/p12-shell-tests.py']
+          removed = ['.github/workflows/p12-alias-repair-preparation.yml', '.github/workflows/p12-source-export.yml', '.github/workflows/p12-native-shell-diagnostic.yml', 'scripts/_prepare_p12_alias_repair.py', 'docs/capability-composition/p12-alias-repair.patch', 'docs/capability-composition/p12-alias-tests.py', 'docs/capability-composition/p12-shell-tests.py', 'scripts/diagnose-p12-cmd-native-path.py', '.github/workflows/p12-native-path-api.yml']
           assert all(not pathlib.Path(path).exists() for path in removed)
           paths = subprocess.check_output(['git', 'ls-files', '-z']).decode('utf-8').split('\0')
           inputs = {p: hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest() for p in paths if p.endswith('.py') or p == 'requirements-source.lock'}
-          identity = {'head': os.environ['EXPECTED_HEAD'], 'tree': subprocess.check_output(['git', 'rev-parse', 'HEAD^{tree}']).decode().strip(), 'evidence_mode': 'COMMITTED_SOURCE_REGRESSION', 'inputs': inputs, 'p12_authorized': False, 'retirement_authorized': False, 'production_acceptance': False, 'known_open_case': 'SandboxCloseoutTests.test_absolute_unicode_workspace_path_in_cmd_runs_inside_private_copy'}
+          identity = {'head': os.environ['EXPECTED_HEAD'], 'tree': subprocess.check_output(['git', 'rev-parse', 'HEAD^{tree}']).decode().strip(), 'evidence_mode': 'COMMITTED_SOURCE_REGRESSION', 'inputs': inputs, 'p12_authorized': False, 'retirement_authorized': False, 'production_acceptance': False, 'required_native_case': 'SandboxCloseoutTests.test_absolute_unicode_workspace_path_in_cmd_runs_inside_private_copy'}
           (out / 'identity.json').write_bytes((json.dumps(identity, sort_keys=True, indent=2) + '\n').encode('utf-8'))
           print('P12_INPUT_IDENTITY=' + json.dumps({k: v for k, v in identity.items() if k != 'inputs'}, sort_keys=True))
           print('P12_INPUT_FILE_COUNT=' + str(len(inputs)))
@@ -67,6 +67,8 @@ jobs:
               ('source-authority', [sys.executable, 'scripts/check-source-authority.py']),
               ('committed-mirrors', [sys.executable, 'scripts/sync-generated-sources.py', '--check-committed']),
               ('historical-diagnostic-contracts', [sys.executable, '-m', 'unittest', 'discover', '-s', 'tests', '-p', 'test_p12_retirement*.py', '-v']),
+              ('native-directory', [sys.executable, '-m', 'pytest', '-q', '-ra', 'tests/test_sandbox_cmd_directory_p12.py', 'tests/test_foundation_closeout.py::SandboxCloseoutTests', '--junitxml=' + str(out / 'native-directory.xml')]),
+              ('version-compatibility', [sys.executable, '-m', 'pytest', '-q', '-ra', 'tests/test_composition_activation_store_p7b2.py', '--junitxml=' + str(out / 'version-compatibility.xml')]),
               ('freeze-and-golden', [sys.executable, '-m', 'pytest', '-q', '-ra', 'tests/golden/p19_r2', '--junitxml=' + str(out / 'golden.xml')]),
               ('alias-and-bootstrap', [sys.executable, '-m', 'pytest', '-q', '-ra', 'tests/test_sandbox_workspace_alias_p12.py', 'tests/test_sandbox_shell_bootstrap_p12.py', '--junitxml=' + str(out / 'repair.xml')]),
               ('unchanged-product', [sys.executable, '-m', 'pytest', '-q', '-ra', 'tests/test_composition_execution_manifest_p7d1.py', 'tests/test_tool_manifest_loading_p8.py', 'tests/test_composition_grant_authority_p7d2.py', 'tests/test_composition_step_execution_p7d1.py', 'tests/test_foundation_closeout.py', '--junitxml=' + str(out / 'product.xml')]),
diff --git a/docs/p19-r2/m6/VERIFICATION_PLANE_FREEZE.json b/docs/p19-r2/m6/VERIFICATION_PLANE_FREEZE.json
index 09bdf20..ab8cbb5 100644
--- a/docs/p19-r2/m6/VERIFICATION_PLANE_FREEZE.json
+++ b/docs/p19-r2/m6/VERIFICATION_PLANE_FREEZE.json
@@ -1,5 +1,5 @@
 {
- "verification_plane_version": "1.15",
+ "verification_plane_version": "1.16",
  "baseline_sha": "9a3344de9fe468fa845d2ff501166484439b8ec4",
  "store_schema_version": 33,
  "contract_schema_versions": {
@@ -89,7 +89,7 @@
   "src/total_gateway/verification_repair_coordinator.py": "089efa6a4c888d4e06d4bdd5044a0adc283d69648463a859f42e2c063f0187f6",
   "src/total_gateway/verification_repair_policy.py": "bb2ac036d01a516729d43cc068b37bb3e92f71983004275937fcfab82b2e9eaf",
   "src/total_gateway/verification_plan_executor.py": "41c6d9fb6d98847d447fc808dcc9b22776cac3889b0692bc0287de7b50022144",
-  "src/total_gateway/verification_plane.py": "97da3c554415958e9225aeb97aa0d919b75f8fdcada506e3afb9d21b7a7b8311",
+  "src/total_gateway/verification_plane.py": "6010e8e96a6b6f4dc0700c8d99094b3179f4d3c19e1a12bad622c6b11181ea94",
   "src/total_gateway/verification_readiness.py": "3c1e7f89fb958ba82cb2aed7de85a237a007717839727a5146a3d29d532a8a39",
   "src/total_gateway/verification_failure_evidence.py": "955b1d3c2e77b2ec229ebdd8da612f8ce26794d6fdfc849967ffaaef6fd73dea",
   "src/total_gateway/verification_registry.py": "afab7caae8079c241953c42d85945fbeb5b106114e16963219c1f8c5524b310a",
@@ -108,7 +108,7 @@
   "src/total_gateway/tool_source_world.py": "a68629be9f24dfad6a5e227e6cd351d45c48bcfe3cc3f2353dbaddac64e8e4d8",
   "src/total_gateway/tool_manifest_evolution.py": "3c07cc4f24be95942eccf3bd65022103e3b5cdadc157fe208b4af67705221030",
   "src/source_authority/validator.py": "a4a4aec4710c80546f9e3ce056b6816e4fb0e2ffc1af99fa2f198c8de3a51f07",
-  "src/omni_body_skill/tools/sandbox_runtime.py": "246231859f8e18bc9985213b0bcc65c6d1e3cc463de9cabc25dd0610ae14ad17",
+  "src/omni_body_skill/tools/sandbox_runtime.py": "09f3af8bed20d4e8e5b5dabd355fe454ca90c514e53b4d4a0d8d6c788a6c7f90",
   "scripts/_tool_source_build_worker.py": "e42b8985ca4178e9b35115a032f34df6535f2dc575f94873c5c1682add01240f",
   "scripts/build-tool-source.py": "85409d4d7626ad596603bfea10514fbf6ce5e880401b846b29f206b295220e28",
   "scripts/stage-tool-source.py": "81f00c22d9b4e16639a19a580ee275a8ae64bd7ce4b2255294f296abec69c84c",
diff --git a/readable-python-source/omni_body_skill/.tiangong-generated-source.json b/readable-python-source/omni_body_skill/.tiangong-generated-source.json
index 0ff179a..b091d60 100644
--- a/readable-python-source/omni_body_skill/.tiangong-generated-source.json
+++ b/readable-python-source/omni_body_skill/.tiangong-generated-source.json
@@ -3,6 +3,6 @@
   "mapping_id": "omni-body-runtime",
   "schema": "tiangong.generated-source-marker.v1",
   "source": "src/omni_body_skill",
-  "tree_sha256": "9ecc5f9ad7babd9447fcac7287c98f66855c276f5f32422b4e75d22527531be6",
+  "tree_sha256": "8243cd6770e9f022e202212a0f78ddae9b4b1b959bc615cfd1bf9415e9fba0c7",
   "warning": "Generated mirror. Edit the authoritative source and run scripts/sync-generated-sources.py --write."
 }
diff --git a/src/omni_body_skill/tools/sandbox_runtime.py b/src/omni_body_skill/tools/sandbox_runtime.py
index a65665e..20e91e2 100644
--- a/src/omni_body_skill/tools/sandbox_runtime.py
+++ b/src/omni_body_skill/tools/sandbox_runtime.py
@@ -369,6 +369,80 @@ def _rewrite_workspace_paths(command: Sequence[str] | str, real: Path, sandbox:
     return [rewrite_item(item) for item in command]
 
 
+def _prepare_windows_cmd_initial_directory(
+    command: Sequence[str] | str,
+    *,
+    cwd: Path | str,
+    workspace: Path | str,
+    comspec: Path | str,
+) -> Sequence[str] | str:
+    """Translate only an initial literal CMD cd into the bound private tree.
+
+    CMD's absolute-name canonicalization can query inaccessible ancestors even
+    when the target directory is accessible to AppContainer. For the FIRST cd,
+    the native process cwd is known: relpath(target, cwd) selects the same private
+    directory without that ancestor walk. Later commands, flags, expansion,
+    redirection and exit behavior are not interpreted or rewritten here.
+
+    This is not a general shell parser. Ambiguous forms and non-CMD executables
+    are left unchanged, as are targets outside the existing brokered workspace.
+    The caller invokes this only after copying/link checks and path rebinding,
+    immediately before launching with that exact private cwd.
+    """
+    import ntpath
+
+    if not isinstance(command, str) or any(ch in command for ch in '\r\n\0'):
+        return command
+    launch = re.fullmatch(
+        r'(?P<exe>"[^"\r\n]+"|[^"\s]+)(?P<gap>[ \t]+)'
+        r'(?P<flags>(?:/[dDsS][ \t]+){0,2}/[cC][ \t]+)(?P<body>.+)',
+        command,
+    )
+    if launch is None:
+        return command
+    executable = launch['exe'].strip('"')
+    def norm(value: Path | str) -> str:
+        return ntpath.normcase(ntpath.normpath(str(value)))
+
+    if (re.match(r"^[A-Za-z]:[\\/]", str(comspec)) is None
+            or norm(executable) != norm(comspec)):
+        return command
+    switches = launch['flags'].lower().split()
+    if len(switches) != len(set(switches)):
+        return command
+    body = launch['body']
+    wrapped = body.startswith('"') and body.endswith('"')
+    inner = body[1:-1] if wrapped else body
+    initial = re.match(
+        r'(?P<prefix>[ \t]*@?(?:cd|chdir)[ \t]+(?:/d[ \t]+)?)'
+        r'"(?P<target>[^"\r\n]+)"(?P<suffix>.*)\Z', inner, re.IGNORECASE,
+    )
+    if initial is None:
+        return command
+    suffix = initial['suffix'].lstrip(' \t')
+    if suffix and not (suffix.startswith('&&') or suffix.startswith('||')
+                       or suffix.startswith('&')):
+        return command
+    target = initial['target']
+    if any(ch in target for ch in '%!^&|<>*?'):
+        return command
+    values = (str(cwd), str(workspace), target)
+    if any(re.match(r'^[A-Za-z]:[\\/]', value) is None for value in values):
+        return command
+    try:
+        private_root = norm(workspace)
+        if any(ntpath.commonpath((private_root, norm(value))) != private_root
+               for value in (cwd, target)):
+            return command
+        relative = ntpath.relpath(target, str(cwd))
+    except ValueError:
+        return command
+    rewritten = initial['prefix'] + '"' + relative + '"' + initial['suffix']
+    if wrapped:
+        rewritten = '"' + rewritten + '"'
+    return command[:launch.start('body')] + rewritten
+
+
 def _posix_preexec(limits: SandboxLimits):
     def apply() -> None:
         import resource
@@ -532,6 +606,12 @@ class SandboxRunner:
             sandbox_cwd.mkdir(parents=True, exist_ok=True)
             rewritten = _rewrite_workspace_paths(command, self._workspace_input, sandbox_workspace)
             if os.name == "nt":
+                system_root = os.environ.get("SystemRoot") or os.environ.get("WINDIR")
+                if system_root:
+                    rewritten = _prepare_windows_cmd_initial_directory(
+                        rewritten, cwd=sandbox_cwd, workspace=sandbox_workspace,
+                        comspec=Path(system_root) / "System32" / "cmd.exe",
+                    )
                 rewritten = _prepare_windows_utf8_shell_command(rewritten, cwd=sandbox_cwd)
             limits = SandboxLimits(
                 timeout_seconds=max(1, int(timeout_seconds or self.limits.timeout_seconds)),
diff --git a/src/total_gateway/verification_plane.py b/src/total_gateway/verification_plane.py
index 0905c1b..19f9cae 100644
--- a/src/total_gateway/verification_plane.py
+++ b/src/total_gateway/verification_plane.py
@@ -10,6 +10,6 @@ fails with VERIFICATION_PLANE_FREEZE_CHANGED otherwise).
 
 from __future__ import annotations
 
-VERIFICATION_PLANE_VERSION = "1.15"
+VERIFICATION_PLANE_VERSION = "1.16"
 
 __all__ = ["VERIFICATION_PLANE_VERSION"]
diff --git a/tests/golden/p19_r2/test_freeze_and_guards.py b/tests/golden/p19_r2/test_freeze_and_guards.py
index 1023b74..12e9590 100644
--- a/tests/golden/p19_r2/test_freeze_and_guards.py
+++ b/tests/golden/p19_r2/test_freeze_and_guards.py
@@ -7,7 +7,7 @@ Guards (M6 §7/§8) — enforced with AST/contract scans:
 - exactly ONE store schema authority constant
 - CompletionDecision construction lives ONLY in completion_gate.py
 - no standalone repair runtime/daemon entry point
-- the single Verification Plane version source exists and is "1.15"
+- the single Verification Plane version source exists and is "1.16"
 
 Freeze guard (M6 §23/§24): the freeze manifest records the authority
 surface hashes; any change fails with VERIFICATION_PLANE_FREEZE_CHANGED
@@ -149,12 +149,12 @@ class ArchitectureGuardTests(unittest.TestCase):
             VERIFICATION_PLANE_VERSION,
         )
 
-        self.assertEqual(VERIFICATION_PLANE_VERSION, "1.15")
+        self.assertEqual(VERIFICATION_PLANE_VERSION, "1.16")
         # the literal must appear in exactly ONE src module
         holders = [
             path.relative_to(ROOT)
             for path in _iter_py_files()
-            if '"1.15"' in (
+            if '"1.16"' in (
                 path.read_text(encoding="utf-8")
             )
             and path.name == "verification_plane.py"
@@ -204,14 +204,14 @@ class VerificationPlaneFreezeGuardTests(unittest.TestCase):
             ),
             "golden_corpus_sha256": self._corpus_sha(),
             "golden_trace_version": "1",
-            # Every 1.15 execution, result-schema and verification authority
+            # Every 1.16 execution, result-schema and verification authority
             # file is content-hashed.  Semantic drift in the runtime chain,
             # store/binding/coordinator/executor/readiness/fencing/successor
             # trips the freeze even when the schema version is unchanged.
             "authority_surface_sha256": self._authority_surface(),
         }
 
-    #: The authority surface frozen at 1.15; all inherited 1.10 and earlier entries remain covered.
+    #: The authority surface frozen at 1.16; all inherited 1.10 and earlier entries remain covered.
     AUTHORITY_SURFACE_FILES = (
         "app/backend/tiangong-backend/v3/fact_kernel/__init__.py",
         "src/contracts/execution.py",
diff --git a/tests/test_composition_activation_store_p7b2.py b/tests/test_composition_activation_store_p7b2.py
index 13fae8e..8dc9fae 100644
--- a/tests/test_composition_activation_store_p7b2.py
+++ b/tests/test_composition_activation_store_p7b2.py
@@ -181,9 +181,10 @@ def test_p7b2_explicitly_advances_store_and_p19_compatibility() -> None:
     # P7C.1 adds v32 authorization receipts and P7D.2 adds the current v33
     # continuation/attempt chain. P8 explicitly advances the frozen plane to
     # 1.6; P9 source retention advances it to 1.7. P10 explicitly advances
-    # the learning/telemetry and native-path surface to 1.14; Store stays v33.
+    # the learning/telemetry and native-path surface to 1.14. P12 explicitly
+    # advances native shell path compatibility through 1.15 to 1.16; Store stays v33.
     assert STORE_SCHEMA_VERSION == 33
-    assert VERIFICATION_PLANE_VERSION == "1.14"
+    assert VERIFICATION_PLANE_VERSION == "1.16"
 
 
 def test_v29_store_migrates_additively_through_v30_to_current() -> None:
