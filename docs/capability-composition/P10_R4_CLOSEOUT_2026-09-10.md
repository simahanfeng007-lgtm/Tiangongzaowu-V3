# P10 R4 工程收尾

用户于 2026-09-10 要求开始 P10 收尾，真实部署仍在其他电脑。本批提前执行
可独立完成的工程门禁和修复，不把 R3-C 的部署观察或独立复核标为完成。

## 固定基线与退出矩阵

- main：`b1ea3e9d511aae9cabcc816e071ad3da645ae753`。
- 首轮候选：`11a2e267b130402f743ef7d228ca1b14f162c3ec`。
- 草稿 [PR #75](https://github.com/simahanfeng007-lgtm/Tiangongzaowu-V3/pull/75)。
- 完成条件：最终候选上的 Architecture、完整 Python、Node、P14、P19
  Windows/Ubuntu 适用门禁通过；核对原始日志、跳过原因、源码与工作流身份。
- 合并仍需 R3 退出复核。部署或独立认可的代表性窗口、起止残留/归属与在途
  来源证据尚未齐全。保留旧兼容面、历史记录和待迁移项；不进入 P11。

## 首轮发现与修复

P19 Golden 在 Windows/Ubuntu 均为 55 passed。后续 M0–M5 扩展组在两平台
均为 1 failed、330 passed：`test_composition_activation_store_p7b2.py` 仍要求
Verification Plane 1.7，而 P10 已在版本源、AUTHORITY_MAP、冻结清单和 Golden
guard 中明确推进到 1.13。该测试未纳入原 P10 focused 组，因此此前定向通过
未发现这处遗漏。

本批将该兼容测试的精确预期同步到 1.13，Store v33 断言不变，并将整个文件
加入 P10 focused 选择。修复后的本机该文件 + 完整 Golden 共 67 passed、
0 failed、0 skipped。产品源码、Source Authority、冻结清单、指纹、权限和
Golden 基线没有修改。

现有 Architecture PR 门禁缺少 Node 回归，本批新增独立 Windows/Ubuntu
Node 24 job：从锁文件安装 renderer 依赖，检查五个桌面入口语法，并运行
tests 下全部 `.mjs`、`test_*.js` 和 `.test.cjs` 用例。原 Python、P14、P19
工作流的测试范围和退出要求保持不变。

新增 Node CI 首轮发现 checkout 默认未下载 LFS 对象，真实 VRM 样本实际读到
的是 Git LFS 文本指针，两平台同一肩宽测试失败。本机已具有实际样本，因此
本机通过未暴露该配置缺口。Node job 显式开启 LFS checkout，保留真实样本的
精确跨度断言；不删除样本测试、不把指针误判为缺失后跳过，也不更改模型文件。

## 本机预验证

首轮 Node 因未安装 renderer 依赖，6 个文件无法加载 `three`。执行
`npm ci --omit=dev --ignore-scripts --no-audit --no-fund` 后完整 Node 回归为
234 passed、2 skipped、0 failed；五个桌面入口语法检查通过。未改锁文件。
该结果绑定 `11a2e26`，输入文件哈希前后相同。

原候选的本机完整 pytest 与三个远端工作流继续运行并保留结果；不得把运行中
状态当成通过。修复后的最终候选须重新取得实际终态。原始本机记录保存在
`output/p10-r4-closeout-20260910/`，修复定向 JUnit 在独立收尾工作区
`output/p10-r4-plane-compatibility.xml`。最终远端结果记录在 PR #75 的描述中，
必须以其完整 head 与各工作流终态核对，不能复用首轮失败候选的通过项冒充。

P10 工程阶段尚未合并，合并比例仍是 10/18（55.6%）。真实模型任务、正式签审、
P8 打包补丁和生产回退等旧验收债继续保留；短时 fixture 不证明生产零使用。
