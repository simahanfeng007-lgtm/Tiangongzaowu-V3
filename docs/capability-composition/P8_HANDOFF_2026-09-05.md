# 天工造物 V3 v1.2：最新进度与交接

交接日期：2026-09-05（北京时间）。本次仅同步 WIP 并停止，等待用户在聊天里更新任务。

## 一、结论

P0–P7D.2 已作为本轮已合并基线；P8 的离线构建、源码绑定与启动校验基础已经实现一部分，但完整启动仍失败，P8 尚未完成、未合并。P9–P17 均未开始。不能按测试数量推算整个母版的完成百分比。

本次收尾只做专项、官方镜像同步、Source Authority 和冻结守卫；没有运行完整 Python/Node 回归，没有继续新功能、真实模型批量评测或新一轮隔离构建。源码发布、执行授权及 A1+ 权限均未开放。

## 二、线上与本地位置

- 仓库：https://github.com/simahanfeng007-lgtm/Tiangongzaowu-V3
- 工作分支：`codex/capability-composition-p8-source-evolution-v1`
- 现有 Draft PR：https://github.com/simahanfeng007-lgtm/Tiangongzaowu-V3/pull/73
- 本次交接提交包含本文件；精确同步 SHA 以交付消息及 PR 的交接评论为准。接手必须重新读取远端 HEAD，不可把以下旧 SHA 当成最新代码。
- 本次只读核实的 main：`9a3344de9fe468fa845d2ff501166484439b8ec4`。
- 本次同步前远端旧头：`9476b0af1a27a530f89da9ee402d5d5e22c43b95`。其历史 CI 全绿只适用于该旧提交。
- 最近一次真实隔离构建候选：`5882fb4af7ed4337f7d508ccbce0ab2b9e075964`，不是本次交接头。
- P8 本地工作树：`C:/Users/77571/Documents/天工造物v3-p8`。
- 原用户工作树 `C:/Users/77571/Documents/天工造物v3` 保持不动，不要把两个工作树混用。

## 三、已经做到哪里

1. 基于不可变 Git 候选，做 Source Authority、源码字节/依赖/编译输入绑定及差异检查。
2. 候选构建进入现有 Windows AppContainer，网络拒绝；候选源码不导入父进程。仍使用唯一 `v3.fact_kernel.compile_manifest`。
3. 构建得到可校验的独立版本 ZIP、逐文件清单及 Manifest；可解包到独立新目录并重验原 ZIP，没有切换生产指针。
4. Gateway 启动前校验明确的源码根、发布清单、Skill 根、镜像、只读文件和模块来源；冻结字节码只按数据保留，不允许作为候选模块导入。
5. Omni 包装器固定已选择的源码目录，不随后续环境变量静默热切换；这不等于未来所有懒加载字节均已证明。
6. 已有真实隔离构建成功证据；真正的完整 Gateway READY/shutdown/source-lock 闭环仍未通过。
7. 本次把已验证的 Windows 原生物理路径检查下沉到共用安全模块，修复已审计的 Skill、Life、通信、后端、密钥与部分工作区初始化入口；拒绝身份校验失败时回退到字符串路径。
8. 开发源码 readiness 支持清单和源码目录分离，必须使用指定根并核对真实二进制/源码入口哈希；指定错误根不得自动换目录，生产二进制选择不得用源码根覆盖。

## 四、本次证据（区分通过、失败与待验收）

| 检查 | 结果 | 证据文件（位于本地 output/p8-evidence） |
|---|---|---|
| 真实隔离构建 14，旧候选 5882fb4 | 成功；AppContainer / network denied；9.843 秒 | foundation-isolated-build-14.json |
| 真实启动探针 6，同一旧候选 | 失败；Skill 目录严格 DOS 路径解析 WinError 5；Gateway 尚未启动 | foundation-isolated-startup-6.json |
| 本次合并范围专项 | 143 passed / 3 subtests，146.07 秒 | startup-path-checkpoint-focused.xml |
| 密钥与签名权威专项 | 14 passed | startup-key-path-focused.xml |
| readiness 目录绑定专项 | 9 passed | readiness-source-root-focused.xml |
| 通信/暂存/桌面入口专项 | 31 passed / 4 subtests | service-startup-path-focused.xml |
| 官方镜像、Source Authority、正常冻结守卫 | 通过 | 详细账本与 startup-path-checkpoint-freeze.xml |
| Windows 全启动故障注入 | 失败，当前阻断见下一节 | startup-dos-denial-3.xml；交接精确头复验见 PR 评论 |
| 本次交接头完整本地/远端门禁 | 未完成；没有用旧 CI 或专项代替 | 待 P8 大步骤收尾 |

上述专项存在覆盖重叠，不要相加成产品任务数量。这些是提交前同代码快照的专项证据，不是“所有精确 HEAD 门禁已通过”。

关键 SHA-256：

- 构建 14 报告：`ebe4a27d0b12f3dbbfb12ff715ff6683aa9636fdb5be33919587930ad99d0c18`
- ZIP：`7121e7e5946ba8b5cf5556ba386904fffb058108b32419d03e9f63d7db82391c`
- 启动 6 报告：`2907ceac81a6c5197c836699a5ff902ee8b8328308c9fc164c83c1d5402bb5f9`
- 143 项专项 XML：`d37bcef8bb5fb93e88b4f4fb6a1974d57dcc26a5c8f635ba465ce43d3fdba182`
- 故障注入第 3 次 XML：`1576dde9b97104e44e2a524d95239789db5485394aec0f590f6420915eba09f6`

原始大体积构建报告、ZIP 及 XML 留在上述本地证据目录，未作为发布包上传。线上保留代码、交接文档、摘要/哈希与复现入口；若云端拿不到原件，必须请求附件或在受支持宿主上重新产生证据，不得假称已读取本地原件。

## 五、当前精确断点与下一步

`tests/test_source_startup_paths_p8.py` 在测试范围内拒绝 `Path.resolve(strict=True)`，用于快速定位已证实的 AppContainer DOS-volume API 兼容问题。它不是在生产里猴子补丁，也不是 OS 隔离证明。

当前已越过 Life/Communication/嵌入式后端及受保护密钥读入，失败于：

```text
GatewayRuntime.start
  -> GatewayOrchestrationWorker.from_runtime_config
  -> GatewayOrchestrationWorker.__init__
  -> src/total_gateway/orchestration.py:457
     workspace_root.resolve(strict=True)
```

该测试仍保留真实失败，没有 skip/xfail，没有改成较弱断言。此 WIP 不具备合并资格，Windows CI 可能据此失败。

用户明确继续后，建议按以下顺序：

1. 重新获取线上 main、工作分支、PR Head 和 CI 的真实状态，先接收用户新增要求。
2. 完整审计 `orchestration.py` 和 `omni_grant_authority.py` 及相关调用，完成剩余启动路径批次。前者约 5,938 行，目前仅完成 1–779 行及部分调用段的本轮重读；后者约 3,382 行，完整审计未完成。不能把 rg 命中当作完整审计后直接批量替换。
3. 相关库审计已完成并有本次修复，但非启动期 strict 路径调用仍需按实际失败逐一审计：Composition preflight、Omni 授权工作区、旧兼容输入/输出路径等。不得全局修改 pathlib、扩大 ACL 或引入权限旁路。
4. 快速启动专项通过后，统一官方镜像同步、提交一个候选，再做一次真实隔离构建和启动探针，观察 READY、关闭、前后源码一致。不要每改一行就打包一次。
5. 然后补 P8 的证据合同执行、审查/Source 发布控制器、运行中 X/X+1 锁定与新 generation/replan。新源码不得修改旧 Run 的 Manifest。
6. 审计 99 项 effective-risk 降级、42 项新进入 A0 的候选差异；它们只是暴露的问题，未批准。发布清单 790/290 与源码编译 791/291 的差异仍需解释。
7. 完成真实 P7/P8 80–100 task、跨模型矩阵和其他母版要求。模型、弱模型、调用预算以及生产 telemetry 仍待确认；不得自行运行付费大批量评测。
8. P8 整个大步骤完成后，才做完整 Python/Node、P14、P19 Golden、Ubuntu/Windows 门禁，精确头一致、证据齐备后合并；验证 main 包含合并，再开始 P9。

## 六、后续阶段

P9 Skill Source；P10 Life Learning Cutover；P11 Formal Shadow；P12 Static Skill Planner 下线；P13 旧 Learning/Registry 下线；P14 动态能力默认；P15 Source Drift/Experience Revalidation；P16 150-round/restart/reconnect；P17 清理与最终权威审计。

全部 NOT STARTED。旧链删除必须先有冻结、真实零生产调用、Shadow 和稳定性证据，不能先删后补。完整母版与产品评测不是本次专项可替代的。

## 七、母版和交接规则

母版文件：`天工造物V3_世界理解驱动ToolSkillSource组合执行体系_详细工程实施计划_v1.2_全链源码审计版.txt`。

本地原件：`C:/Users/77571/Documents/xwechat_files/wxid_8768717686911_3713/msg/file/2026-09/天工造物V3_世界理解驱动ToolSkillSource组合执行体系_详细工程实施计划_v1.2_全链源码审计版.txt`。

SHA-256：`691e857e3a7f75d9107606bbffe2919bd484c03e25f1255cebd1bb069f89f895`（本次重新校验一致）。云端如不能读取，应附上原件，不得只凭本摘要重建完整验收范围。

始终保持唯一 Gateway/Runtime/WorldState/Memory SSoT/Action Registry/Policy-Ticket-Grant/Effect-Fact/P19/Completion。A0 read/verify 上限不变；候选代码只在现有严格 OS 隔离子进程加载；禁止恢复 PR #66、强推、admin merge、静默放权和降低测试。

停止是用户主动交接，不是“目标完成”或“技术阻塞”。本轮不继续修 CI、不轮询到全绿、不进入 P9。GitHub 已触发的 CI 可自行运行，但不会替代编码任务继续完成 P8–P17。若应用里的总目标仍显示 active，用户需在目标界面暂停/更新后再启动，不能伪造 complete/blocked 状态。
