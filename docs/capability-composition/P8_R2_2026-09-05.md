# P8-R2: native startup acceptance and trusted-probe hardening

## Scope and identity

Main was reread as `9a3344de9fe468fa845d2ff501166484439b8ec4`.
R1 source candidate is `e99b4a2b1764b55dc74dbd04ce83c9473275d3dc`,
source tree `bd10b3ce0647e1ab720754adaca2234f87e3eb79`.
The supplemental workflow was added at `c1abbb1d66aa05047317ccb15e6e923be0f66b28`.
PR #73 stays Draft. No main merge, publication, A1+ admission or P9 work.
Workflow identity, installed trusted probe and tested candidate are distinct.

## Explicit Verification Plane 1.6 addendum

This extends the existing item 19 offline startup probe, not task Completion.
A retained `cleanup_error`, `error`, `error_type`, `failed_phase` or `traceback`
field contradicts child success, even if empty. The parent rejects that record
while retaining the original process and child evidence. All approval flags
stay false. Source identity, P19 oracle, Golden corpus and permissions do not
change. Only the existing parent-probe hash is refreshed by the official
`UPDATE_FREEZE=1` generator, followed by a normal guard run without that flag.
No covered source is removed and no Golden expectations are rewritten.

The trusted parent strictly resolves its own newly-created temporary directory
before staging. A Windows temporary-path alias can otherwise fail the stager's
canonical comparison before any child runs. The stager's canonical, link,
junction, inventory, read-only, content and original-ZIP checks are unchanged.
Missing physical identity still fails; no non-strict candidate fallback exists.

## Original Windows observation, not startup acceptance

Run `33956634815`, job `101281040413`, workflow `c1abbb1`, candidate/parent `e99b4a2`:
Windows Server 2025, Python 3.12.10, dependencies from requirements-source.lock.
Startup fault-injection/Life tests: **13 passed, zero skips**.
Real source build: **ISOLATED_BUILD_OBSERVED**.
Startup: **STARTUP_PROBE_FAILED**, before Gateway execution:
`SourceCandidateError: staged source root is not canonical`.
The wrapper's generic containment error is NOT proof containment was absent;
the raw startup report has no child process because staging failed first.

Original artifact `9966617826`, ZIP SHA256:
`2c37a8441ba52dbb5d8093adb95297567c97b58a9e982318c7254bdf85538bc4`.
Contained source-bundle SHA256:
`e49f0c51009e5e19e0d7ebc9cacb506d53ac512b25c0cc0c82231629e945eff0`.
Reports and archive checksums were downloaded and inspected, not inferred from
job labels. The supplemental workflow retains failed evidence as well as passes.

## Regression evidence

Six red-first cases: five contradictory failure-field cases plus one owned
noncanonical-temporary-path case. Each failed against the preceding code.
After repair, local Linux/Python 3.13.5 targeted bundle/staging/probe/Life suites:
**61 passed, 1 Windows-only skipped**. Existing P19 freeze/architecture guard:
**6 passed**. Source authority and official `--check-committed` mirror checks pass.
Full `--check` on a source-only ZIP reports absent ignored build-runtime mirrors;
that is not a full installed-runtime check or a claim that all mirrors exist.
Local dependency versions differ from the release lock; local fixtures are not
native isolation or full-regression acceptance.

## Next execution and exit gate

Pin this repaired trusted parent in the Windows workflow, retain exact R1
candidate/base identities and rerun against that candidate. Print the raw
startup report before aggregate checks so the actual failure is not obscured.
Review the terminal native report and exact-source CI. Required native gate:
actual AppContainer build -> Gateway READY -> successful close -> matching
pre/post source identity, with no failure or approval claims. A skip or green
unit test cannot replace it. P8 remains open even after this integration gate;
permission-delta review, approved publication, live X/X+1 run locks and real
model/task evaluation remain separate P8 obligations.
