# v3 执行、对抗复核与缓存实施记录

基线：main `a372dbc7` + 本地 `44c0cbb`，合入 PR #112 `4456f71e`。
合入保留真实回执、字典别名与结构化 delivery_mode 的修复；自然语言义务和关键词门保持删除。

按已确认计划依次完成：

- [ ] 可恢复多文件提交，崩溃、磁盘错误、并发修改与重复请求验证。
- [ ] 裁判读取完整持久证据，按版本引用、分页和候选全文覆盖。
- [ ] 按有效配置固定执行/裁判/反例模型角色及有限故障切换。
- [ ] 长任务预留复核预算，重复异议澄清和 UI 阶段区分。
- [ ] World 增量定期完整刷新、端点预算与全调用成本度量。
- [ ] 全库、字典及可用能力实测；缺失外部环境逐项记录。
- [ ] Linux 和原生 Windows 最终候选验收与对抗矩阵。
- [ ] 冻结候选证据后推送 PR，通过同一提交必需 CI，再合并 main。

验收范围覆盖当前完整产品。专项通过不等于全库通过，Linux 上的 PowerShell 不等于原生 Windows，历史 CI 不代表本候选。99% 缓存命中率不是完成或合并条件。未达成全部强制条件前不合并 main。
