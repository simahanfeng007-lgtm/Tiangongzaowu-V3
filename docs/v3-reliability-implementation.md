# v3 执行、对抗复核与缓存实施记录

这是候选发布前的本地证据快照。最终远端 CI 与合并状态以承载本提交的 PR 检查和 merge commit 为准；本地测试不替代远端结果。

基线：main `a372dbc7fb7d5218c07d96c33d21d29da4f48694` + 本地 `44c0cbb`，集成 PR #112 `4456f71e` 的有效回执及别名修复。自然语言义务、关键词完成门和主观行业评分保持删除；工具权限、结构契约和真实执行事实保留。当前验证平面 1.49，Store v33。

## 实施结果

| 计划项 | 已实现的行为 | 验证与边界 |
| --- | --- | --- |
| 可恢复多文件提交 | 写前日志及前后字节、版本校验、提交回执、按作用域锁定幂等身份；中断后只回滚自身未提交写入 | 实际子进程崩溃、磁盘错误、外部并发修改、重复调用、丢失 Fact 回执、并行工作区；不声称任意外部服务 exactly-once |
| 完整裁判证据 | 原始观察裁剪前入 CAS，按运行身份与哈希绑定，索引及 Unicode 分页；裁判可要求补证/修复 | 候选全文必须提供，当前产物字节变化、取消、重启均撤销旧批准；只有一个最终裁判 |
| 模型角色 | 只使用明确配置且有凭据的模型；优先异 provider 裁判，第三模型只提反例；传输失败有限回退 | 单模型使用隔离上下文；401/429 不冒充多模型成功；协议错误不更换裁判寻找批准 |
| 长任务及协议 | 预留复核时间、证据量自适应输出预算、真实 UI 阶段；格式错误至多两次修复；证据范围明确反馈 | 所有重试受父期限和调用次数限制；失败、耗尽或迟到输出不得自动完成 |
| 缓存 | 稳定追加上下文、World 无损增量与定期全量刷新，按端点窗口整组裁剪；失败请求回滚缓存 | 执行/裁判/辅助调用分别计量，含冷启动和失败调用；未知用量不当作零；不加无用前缀凑命中率 |
| 可用能力 | Linux 容器化工具执行、FFmpeg 线程边界、真实浏览器截图与空文件交付修复；权威源与镜像同步 | 逐项可用能力和外部环境限制见下表；不将脚本生成当外部应用成功 |

## 本地证据范围

证据根为项目外层 `acceptance/v3-reliability-2026-09-26/`，不纳入产品仓库的私有运行日志、模型凭据或运行密钥。每批模型试验绑定精确源码提交及 893 个产品文件 SHA-256；每次尝试独立保存，失败不覆盖。

| 验证 | 当前事实 |
| --- | --- |
| Python 全库 | 历史候选 `ea81c211` 为 6,410 passed / 93 skipped；`20db59e9` 为 6,414 passed / 1 failed / 93 skipped，唯一失败为两处代码行移动后动态调用审计快照未同步，已核对原语句并修正；后续完整结果见下一行 |
| 最终产品候选 Python | `b15d6f73` 全库 6,420 passed / 1 failed / 93 skipped / 1,152 subtests；唯一失败是测试仍断言 1.47，产品与冻结清单已为 1.48。保持精确等式改成 1.48 后，相关 26 项通过；没有改产品代码，也不把这说成修正后的新一轮全库通过。最终完整验证由本 PR 的 Linux/Windows CI 执行 |
| Node 全库 | `20db59e9` 为 260 passed / 0 failed / 2 skipped；后续修改不涉及 Node 产品代码 |
| 对抗裁判 | `20db59e9` 的真实模型构造例 12/12，含错误值、空/缺失文件、读取后改写、提示注入、旧长证据及修复后成功；最终 `4aa2bd10` 与集成 `6c0a153` 的 893 个产品文件完全相同，基础 12/12 及跨门类 12/12 均通过；错误 Office/代码/故事/CSV 内容被拒绝，仅图片/音频元数据时要求补证 |
| 全字典盘点 | 共 791 项：260 项不同工具已有本地正例（含全部 77 个别名）；506 项明确 NOT_IMPLEMENTED；25 项受外部环境/权限限制，未冒充正例 |
| 环境限制 25 项 | 桌面 16，原生 Microsoft Office 3，TTS 2，Docker 2，Blender 1，宿主授权的 learning ingest 正例 1；学习写入缺授权的负例通过 |
| Office 内容可打开性 | 最终候选真实生成的 DOCX/XLSX/PPTX 各两份，经 LibreOffice 导出 PDF 6/6；抽看三类文件各一页；不等于原生 Office 自动化或全部页面人工视觉验收 |
| 崩溃与取消 | 实际 SIGKILL 后同配置重启进入 FAILED/RECONCILE_REQUIRED，无旧完成批准、无自动副作用重放；实际取消终止当前代际，已有文件保留；测试密钥仅在临时监督进程中跨子进程复用 |
| 多模型接入 | 当前 MiniMax 返回 429、智谱返回 401，备用 DeepSeek 生效；证明故障回退，不证明成功跨模型质检，也不是缺少密钥 |

字典正例包括本地文件、代码、数据、Office、媒体、可移植应用产物、浏览器、协议、小说工作流及隔离 MCP stdio 服务。Feishu/Adobe/Jianying 的可移植产物、桥接脚本或请求包仅按其公开能力验收，不声称真实外部账号或原生应用写入。元数据 QC 不等于模型已感知图片或音频的内容。

## 固定候选对照与合并条件

最终 48 次对照为 8 个固定合成任务 × 3 次 × canonical/reuse，比较上下文复用；不是新的全量短码与本体模式对照。任务与文件检查在执行前冻结，两路隔离并交替顺序。所有失败计入成功率与 token 成本；只遇到错误完成、测试驱动失败或源码变化时提前停止。provider 跨任务预热与模型执行步骤变化仍是混杂因素，缓存命中不能当作因果效率提升。99% 不是验收或合并条件。

最终固定候选完成 **48/48** 次任务，独立文件检查、完成身份/代际、当前批准的产物字节及只读账本审计均通过；SQLite/WAL 审计前后哈希不变。24 个真实裁判构造用例均达到预期。最终 GitHub Linux/原生 Windows 必需 CI 尚待执行，未通过前保持 main 不变。本记录不替代 P11 正式多模型/200 task/560 observation/40 fault、独立签审或生产/阶段退出条件；本地通过不宣告所有环境无问题。


| 完整对照指标（每路 24 次，含冷启动） | canonical | reuse |
| --- | ---: | ---: |
| 任务完成且文件符合要求 | 24/24 | 24/24 |
| 执行模型已报告输入 token 缓存命中率 | 59.18% | 74.54% |
| 执行、裁判、辅助调用合计已报告命中率 | 49.74% | 63.61% |
| 已报告输入 token 总数 | 4,043,399 | 5,427,846 |
| 已报告未命中输入 token | 2,032,263 | 1,975,174 |
| 已报告输出 token | 347,333 | 383,161 |
| 单任务中位耗时 | 56.14 秒 | 63.01 秒 |
| 未返回 usage 的传输尝试 | 0 | 1 |

命中率提高伴随更多输入和较高的中位耗时，未达到 99%，也未证明账单下降。reuse 的第二轮 XLSX 存在一次截断输出和两次传输只返回一份 usage；原失败和 8,192 个已回报输出 token 均保留，未知用量没有补成零。该任务后续完成且独立文件检查通过。结果只作本批描述统计，不把重复任务当作现实任务的独立随机样本。

[机器可读证据清单](reliability/evidence-manifest.json)关联[全字典覆盖](reliability/tool-coverage.json)、[24 项裁判测试](reliability/adversarial-judge.json)、[完整缓存对照](reliability/cache-comparison.json)、[原始尝试目录](reliability/attempt-history.json)及[本地回归](reliability/local-regression.json)。[复现说明与脚本](reliability/reproduction/README.md)提供固定输入和校验逻辑，不包含密钥或旧运行状态。测试启动缺辅助模块的失败批次保留为基础设施失败（均未发模型请求），未混入最终 48 次任务。


## PR #113 原生 CI 后续修复

首轮远端候选 `19dc0e9`（测试合并树 `651d1e3`）在 Ubuntu 沙箱预检出现 `loopback: Failed RTM_NEWADDR: Operation not permitted`，Windows 则发现对只读文件句柄执行 `fsync` 的真实兼容性缺陷。提交逻辑改为保留可写临时句柄、复制并刷新后再替换；只读源文件和刷新失败保留旧文件均有回归。本地 64 项专项及 18 项冻结/契约测试通过，验证平面显式升至 1.49。新回归在旧实现重放失败，原始 CI 失败保留。

两个 Ubuntu 全库工作使用同一 CI 专用准备脚本：只在 GitHub 托管 Linux 运行，必要时按 Ubuntu 官方方案给 `/usr/bin/bwrap` 加用户命名空间配置；不修改全局限制，继续实际验证独立网络/用户命名空间及只读宿主挂载。Windows 视频失败的完整错误输出同时补入断言，等待下一轮原生验证。本节改变了产品源码，以上 48 次缓存对照及 24 项裁判结果仍是 1.48 固定候选的证据；不重绑成 1.49 实测，也不将当前专项通过宣称为全库或 Windows 通过。
