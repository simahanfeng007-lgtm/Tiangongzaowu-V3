# 天工造物 v3.0.3 完整版 — 工程生命体桌面产品

> 一个运行在你电脑上的**工程生命体**：不是简单的聊天助手，而是一个拥有独立身份、记忆、情感、自主意志，会自我反思、自我迭代、主动与你通信，并具备一整套“身体机能”的桌面生命系统。

产品代号：`engineering-organism-v3.0-complete` · 目标平台：Windows 10/11 x64 · 架构：Electron 桌面端 + 单进程生命内核（`127.0.0.1:7184`）

---

## 系统全景

天工造物由一套**单进程总网关**承载：生命内核（LifeKernel）、全能身体技能执行器（Omni Body）、通信模块、策略与安全引擎全部内嵌在同一物理进程内，共享状态库、写租约与事件链。桌面端通过 Electron 提供对话、知识、技能、身体、生命等面板。

```
┌─────────────────────────── Electron 桌面端 ───────────────────────────┐
│ 对话 | 知识库 | 技能 | 运行状态 | 身体/VRM | 生命面板 | 设置           │
└──────────────┬───────────────────────────────────────────────────────┘
               │ 127.0.0.1:7184
┌──────────────▼─────────────── 单进程总网关 ───────────────────────────┐
│ 生命系统(LifeKernel) · 全能身体技能(Omni Body) · 通信(微信/飞书)       │
│ 策略与授权 · 安全沙箱 · 产物与对象库 · 调度编排 · 契约与身份           │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 一、生命系统（LifeKernel）

生命系统的权威实现位于 `src/life_service/`（40+ 模块），打包运行时镜像位于 `app/life-service/runtime314/`。它是产品的核心：一个**持续存活**的生命，而不是被调用才有反应的普通后端。

### 生命状态与生命链

- 独立身份（`life_id`）与灵魂（Soul）配置，实时状态包括“存活 / 成长 / 焦点”。
- 每一次心跳、任务、反思都会写入**生命链（状态时间线）**——带哈希签名的防篡改事件日志（journal），任何篡改都可被检测。
- 生命面板实时投影：今日存活、已完成行动、模型预算、下次心跳、当前焦点等。

### 生命信箱

- 生命会主动投递消息（未读提醒、消息列表）；
- 一键“打开信箱并进入聊天”，消息写入主对话；
- 主动分享受权限与免打扰约束，不绕过消息边界。

### 身份、记忆与上下文

- 身份迁移（`identity_migration`）：从旧版/冻结运行时安全迁移，旧数据可审计、可回放、可切入；
- 记忆（`memory_classification` / `memory_lifecycle` / `memory_migration`）：分类管理、生命周期与迁移，加密存储；
- 上下文（`context_api` / `context_authority`）：编译、授权与投影，生命知道“自己是谁、发生了什么、正在做什么”。

### 自主意志与日程

- 心跳调度器默认每 30 秒驱动一次生命循环（`complete_scheduler`）；
- 自主任务生成与执行（`autonomous_tasks`）：根据自身状态与已选活动提出候选任务，受活动范围（`activity_scope`）与模型预算约束；
- 自主等级可配置，任务失败可恢复、可续跑。

### 反思、迭代与自产能力

- 反思（`reflection`）：定期复盘自身行为与结果；
- 能力自学习（`capability_learning` / `learning_executor`）与能力生命周期（`capability_lifecycle`）：自己长出新的本领；
- 产出物（`artifact_executor`）：行动产生可追踪的产物。

### 情感、气质与活力

- 情绪系统（`affect` / `transient_affect` / `affect_expression`）与稳定气质（`temperament`）；
- 活力评估（`viability`）与状态投影（`panel_projection`）。

### 生命周期与安全切换

- 单写租约（writer lease）：同一时刻只有一个权威写入者；
- 影子模式（shadow）：默认只读观察（`OBSERVE_ONLY`）；
- 切入（cutover）需要签名握手与旧写者停写证据，回滚前校验事件列表完整；
- 兼容旧版（`legacy_adapter` / `legacy_fusion` / `replay`）与身份迁移闭环。

---

## 二、总网关与编排系统（Total Gateway）

`src/total_gateway/` 是单进程的“大脑主干”，监听 `127.0.0.1:7184`，内嵌 Runtime、Life、Communication、Policy：

- **编排（`orchestration` / `coordination`）**：统一调度各子系统，事件与租约协调；
- **执行引擎（`execution_engine` / `effects`）**：运行结果回写与副作用控制；
- **产物系统（`object_store` / `artifact_*`）**：对象库、产物进入/质检（`artifact_qc`、`docx_qc`）/出口（`artifact_egress`）/开放（`artifact_open`）；
- **策略引擎（`policy_engine` / `policy_evidence`）**：授权、证据、Ticket/Grant（`tickets` / `grant_signer`）；
- **任务与连续性（`active_requests` / `continuity` / `response_saga`）**：长任务断点、恢复与 SAGA 一致性；
- **技能治理（`skill_authority` / `skill_selection` / `skill_api`）**：技能准入、选择与调用；
- **事实台账（`fact_ledger`）**：跨系统事实记录；
- **就绪与诊断（`readiness_collector` / `diagnostics`）**：健康探针、运行观测；
- **灵魂备份（`soul_backup`）**：加密 Soul Backup；
- **对象回收（`object_gc`）**：对象库生命周期管理。

---

## 三、全能身体技能系统（Omni Body / Runtime）

执行层位于 `app/backend/tiangong-backend/v3/`，是生命的“身体机能”，包含一组生命机体子模块：

- **事实核（`fact_kernel`）**：知识/事实底座，与知识库系统联动；
- **观察评估（`guancha_pinggu`）**：`观察(guancha)` 与 `评估(pinggu)`，对自身与外部输入做观测；
- **古通层（`gutong`）**：`古通层(gutong_ceng)`、`上下文(shangxiawen)`、`灵魂加载(soul_jiazai)`——跨层上下文与灵魂装载；
- **技能层（`jineng`）**：`肌肉层(jirou_ceng)` 工具执行、`模型适配(moxing_shipei)`（DeepSeek / MiniMax / 谷歌等适配）、`HTTP客户端(http_kehuduan)`、`L4优化观察`、`omni_grant_client`；
- **进化系统（`jinhua`）**：`闭环引擎(bihuan_yinqing)`、`候选生成(houxuan_shengcheng)`、`评估(pinggu)`、`验证审批(yanzheng_shenpi)`、`执行(zhixing)`、`元语映射(yuanyu_yingshe)`、`表达路由(biaoda_router)`、`数学桥接(shuxue_qiaojie)`——让生命能自我进化；
- **免疫代谢（`mianyi_daixie`）**：`免疫(mianyi)`、`代谢(daixie)`、`实验(shiyan)`——防护、资源代谢与实验；
- **治理系统（`zhili`）**：`安全(anquan)`、`版本迁移(banben_qianyi)`、`能力注册(nengli_zhuche)`；
- **自愈系统（`ziyu`）**：`引擎(yinqing)`——异常后的自我恢复；
- **总调度（`zongdiaodu`）**：身体机能统一调度；
- **内置技能（`bundled_skills`）**：`omni_body_skill`（文档/PPT/表格/专业应用/CLI/沙箱等全能工具契约）、`novel-creation`（网文创作全流程）。

其他执行层能力：`知识库(knowledge_store)`、`上下文压缩(context_compactor)`、`上下文续接(shangxiawen_xujie)`、`对话桥接(duihua_qiaojie)`、`回复净化(reply_sanitizer)`、`语音输出(voice_output)`、`身体状态(shenti_zhuangtai)`、`身体设置(body_settings)`、`安全IO(safe_io)`、`权限设置(permission_settings)`、`端点安全(endpoint_security)`、`确认存储(confirmation_store)`、`模型流配置(model_stream_config)`、`工作区设置(workspace_settings)`、`状态同步(zhuangtai_tongbu)`、`权锥线(quanzhuixian)`。

底层内核原语（`tiangong_kernel/l0_primitives`）：自主、上下文、决策、事件、健康、身份、学习、生命周期、记忆、消息、度量、观察、检索、时间、价值。

---

## 四、通信系统（Communication）

`src/communication_service/` 负责多通道接入与投递：

- **微信（WeChat）**：登录（`wechat_login`）、收发（`wechat_inbound` / `wechat_text_outbound`）、文件与媒体（`wechat_attachment` / `wechat_media` / `wechat_file_outbound`）、会话管理（`wechat_session`）、传输控制（`wechat_transfer_control`）、后台 worker（`wechat_worker`）；
- **飞书（Feishu）**：收发（`feishu_inbound` / `feishu_outbound`）、附件（`feishu_attachment`）、路由（`feishu_route`）、worker（`feishu_worker`）；
- **通道治理**：通道权威（`channel_authority`）、投递台账（`delivery_ledger`）、投递分发（`delivery_dispatcher`）、凭证库（`credential_vault`）、原始入站存储（`raw_inbound_store`）、附件隔离（`attachment_quarantine`）、入站收件箱（`inbox`）、影子镜像（`shadow_mirror`）、生产入口（`production_ingress`）、排水（`drain`）。

---

## 五、对话系统（Conversation）

- 对话面板（`conversation-panel.mjs`）与历史记录（`history-block.mjs`）；
- 模型流配置（`model_stream_config`）、对话桥接（`duihua_qiaojie`）、回复净化（`reply_sanitizer`）；
- 正常闲聊与任务完成均视为正常终态，不再误报“任务已停止”；
- 消息、历史与生命信箱内容在统一对话流中呈现。

---

## 六、知识库系统（Knowledge）

- 知识库面板（`knowledge-panel.mjs`）：定位、打开、自动整理、搜索、文档列表；
- 后端知识库存储（`knowledge_store`）与事实核（`fact_kernel`）；
- 文档进入后进入可检索、可整理的知识资产，供生命与用户共同使用。

---

## 七、技能资产系统（Skills）

- 技能面板（`skills-panel.mjs` / `skills-side-block.mjs`）；
- 技能路由索引（`skill_router_index.json`，内置 34 项技能）、技能准入（`skill_authority`）、技能选择（`skill_selection`）；
- 内置技能：Omni Body 全能身体技能、网文创作等；
- 技能注册（`nengli_zhuche`）与能力清单（`capability_manifest.generated.json`）。

---

## 八、运行状态与执行系统（Execute / Runtime Status）

- 运行面板（`execute-panel.mjs`）与运行状态块（`runtime-status-block.mjs`）；
- 运行观测（`run_observation`）、诊断（`diagnostics`）、对象/产物 QC；
- 长任务执行、断点恢复与结果回写（`execution_engine` / `response_saga` / `continuity`）。

---

## 九、VRM 身体与角色系统（Body / Avatar）

- 身体面板（`body-panel.mjs`）、角色面板（`avatar-panel.mjs`）、VRM 展示（`vrm-inspector-panel.mjs`）；
- 3D 形象引擎（`three-vrm-engine.mjs`）：自然站姿按人体生物力学标定（JOSR 2022 / Lee & Jung 2014），表情、口型、手势与实时驱动；
- 肩宽校准（`shoulder-calibrator.mjs`）与生物学对齐技能（`.codex/skills/vrm-alignment/`）；
- 形象资产：`捏脸.html`、`桌面宠物.html`、VRM 导入与 VRC 转码（`vrc-import.js`）、头像资产/存储宿主（`avatar-asset-host.cjs` / `avatar-storage-host.cjs`）；
- 角色记忆：记住上次选择的模型，刷新/重启后恢复。

---

## 十、设置与模型接入系统（Settings）

- 设置面板（`settings-panel.mjs`）与提供商预设（`provider-presets.mjs`）；
- 模型端点绑定与 API 探针（`endpoint_security` / `model_endpoint`），密钥本地加密存储；
- 身体设置、工作区设置、权限设置、模型流配置；
- 主题（6 套）、语音、头像、微信/飞书绑定等。

---

## 十一、安全与沙箱系统（Security）

- `src/runtime_security/`：DPAPI 凭据保护（`dpapi`）、模型端点校验（`model_endpoint`）、票据验证（`ticket_verification`）、安全归档（`archive`）；
- Python/Shell 沙箱：私有工作区、净化环境、资源约束、原子回写（`safe_io` / `sandbox_runtime` / Windows AppContainer）；
- 权限设置（`permission_settings`）与确认存储（`confirmation_store`）；
- 策略引擎（`policy_engine`）与 A5 硬拒绝：敏感操作由确定性策略拒绝，模型不能绕过；
- 回复净化（`reply_sanitizer`）与端点安全（`endpoint_security`）；
- 加密 Soul Backup 与事件链签名校验。

---

## 十二、契约与身份系统（Contracts）

`src/contracts/` 定义了全套机器可校验契约：

- 生命（`life`）、记忆（`memory`）、自主（`agency`）、情感（`affect`）、因果（`causal`）；
- 投递（`delivery` / `delivery_authorization`）、执行（`execution`）、产物（`artifacts`）；
- 策略（`policy`）、安全（`security`）、授权（`authorization`）、身份（`identities`）、范围（`scope`）；
- 影子（`shadow`）、切入（`cutover`）、就绪（`readiness`）、发布（`release`）、可靠性（`reliability`）、状态机（`state_machine`）；
- 主契约：`tiangong.life.api.v2`、`tiangong.desktop.backend.v3`、`tiangong.communication.api.v1`、`tiangong.total-gateway.api.v1`。

---

## 十三、更新与信任系统（Update & Trust）

- 安全更新器（`secure-updater.js`）与更新信任根（`update-trust.json`，Ed25519 公钥绑定）；
- 更新前校验签名与信任根，失败自动回滚；
- 默认关闭线上更新，配置真实公钥、HTTPS 更新源与 Windows 签名发布者后才启用。

---

## 十四、发布流水线（Release）

- `scripts/release-win.mjs`：冻结单进程总网关（PyInstaller）、嵌入后端源树、执行 Runtime/Life/Communication/Policy 契约探针；
- electron-builder + NSIS 生成安装器（`npm run release:win`）；
- `verify-windows-artifacts.ps1`：解包安装器重验 release binding、app.asar VRM 模块闭包、禁发资产与哈希一致性；
- 未配置签名证书时产物明确标记为“未签名候选包”。

---

## 十五、桌面壳与进程生命周期（Desktop Shell）

- `main.js`：Electron 主进程与窗口管理；
- `service-supervisor.js`：拉起/守护单进程网关；
- `runtime-root.js`：运行时根与路径解析；
- `preload.js`：沙箱化 preload（仅依赖 electron），IPC 窄桥；
- 单实例 epoch、本地状态库、生命写租约与模块就绪校验。

---

## 快速开始（源码版）

首次准备内置 Python 运行时：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\setup-source.ps1
```

启动：

```powershell
.\scripts\start-source.ps1
```

（或双击 `start-tiangong.bat`）

验证网关与生命内核就绪：

```powershell
Invoke-RestMethod http://127.0.0.1:7184/ready | ConvertTo-Json -Depth 10
```

正常应返回 `status: READY`、`deployment_mode: embedded`，且 `embedded_modules.life.life_ready = true`。

运行测试：

```powershell
.\scripts\verify-source.ps1 -Full
```

---

## 构建 Windows 安装包

```powershell
cd app
npm run release:win
```

发布流水线会冻结单进程总网关，执行 Runtime / Life / Communication / Policy 契约探针（生命 API 契约不通过即阻断发布），再由 electron-builder + NSIS 生成安装器。未配置签名证书时产物标记为“未签名候选包”。

---

## 目录速览

```
app/                          Electron 桌面端、前端、主后端、生命服务与打包运行时
src/life_service/             生命系统权威实现（身份/记忆/上下文/日程/自主意志/反思/学习）
src/total_gateway/            单进程总网关（内嵌 Runtime/Life/Communication/Policy）
src/communication_service/    通信模块（微信、飞书等接入）
src/runtime_security/         安全与凭据（DPAPI/模型端点/票据）
src/contracts/                全套机器可校验契约
app/backend/tiangong-backend/ 全能身体技能执行层（事实核/观察评估/古通/技能/进化/免疫代谢/治理/自愈）
app/life-service/runtime314/  与权威源码字节一致的打包生命运行时
app/frontend-v2/              前端（对话/知识/技能/身体/生命/设置）
tests/                        顶层回归测试
scripts/                      源码安装/启动/验证/发布流水线
.codex/skills/vrm-alignment/  VRM 生物学对齐技能（站姿/手型标定流程与数据）
```

---

## 版本信息

- 产品：天工造物 v3.0.3 完整版
- 架构版本：`engineering-organism-v3.0-complete`
- 生命 API 契约：`tiangong.life.api.v2`
- 源码基线：`2026-07-22-single-process-continuity-portability-50-final`
